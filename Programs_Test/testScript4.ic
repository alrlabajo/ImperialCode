Embark() {
    Numeral score = 100;

    Thou (score >= 90) {
        Emit("Grade: A");
    } Or Thou (score >= 80) {
        Emit("Grade: B");
    } Or Thou (score >= 70) {
        Emit("Grade: C");
    } Or {
        Emit("Grade: F");
    }
}
