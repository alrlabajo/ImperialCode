Embark() {
   Numeral i, j, rows;
   Emit("Enter the number of rows: ");
   Seek("%d", &rows);
   Per (Numeral i = 1; i <= rows; i++) {
      Per (Numeral j = 1; j <= i; j++) {
         Emit("* ");
      }
      Emit("\n");
   }
   Recede 0;
}
