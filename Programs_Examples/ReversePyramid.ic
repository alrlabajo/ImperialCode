Embark() {
   Numeral rows;
   Emit("Enter the number of rows: ");
   Seek("%d", &rows);
   Per(Numeral i = rows; i >= 1; i--) {
      Per(Numeral j = 1; j <= i; j++) {
         Emit("%d ", j);
      }
      Emit("\n");
   }
   Recede 0;
}