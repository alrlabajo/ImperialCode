Method Numeral sumOfDigits(Numeral num) {
  Numeral sum = 0;
  Until (num > 0) {
    sum += num % 10;
    num /= 10;
  }
  Recede sum;
}

Embark() {
  Numeral number;

  Emit("Enter a number: ");
  Seek("%d", &number);

  Emit("The sum of digits is: %d\n", sumOfDigits(number));

  Recede 0;
}