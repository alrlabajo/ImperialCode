Embark() {
    Decimal num;
    Decimal fact = 1.00;

    Emit("Enter a number: ");
    Seek("%f", &num);

   Per (Numeral i = 1; i <= num; i++) {
        fact = fact * i;
    }

    Emit("Factorial is %f\n", fact);
}