Embark() {
    Numeral n;
    Decimal sum = 0.0, term = 1.0;

    Emit("Enter the number of terms (n): ");
    Seek("%d", &n);

    Per (Numeral i = 0; i < n; i++) {
        sum += term;
        term = term / 3; 
    }

    Emit("Sum = %f\n", sum);
}